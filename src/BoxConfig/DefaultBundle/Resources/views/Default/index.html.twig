{% extends("::base.html.twig") %}

{% block sidebar %}
<div class="row">
    {% render "BoxConfigDefaultBundle:Sidebar:top" with { "slug" : "ide" } %}
    {% render "BoxConfigDefaultBundle:Sidebar:top" with { "slug" : "os" } %}
    {% render "BoxConfigDefaultBundle:Sidebar:register" %}
</div>
{% endblock %}

{% block body %}

<div class="hero-unit">
    <img src="/img/open-box-hi.png" height=50% align="right">
    <h1>Welcome</h1>
    <p>Welcome to box-config.com. A site dedicated to your development environment. Find out what others are using
    on their system.</p>
    <p><a class="btn btn-primary btn-large" href="{{ path("BoxConfigDefaultBundle_about") }}">Learn more &raquo;</a></p>
</div>

{% endblock %}
