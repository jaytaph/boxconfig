<h1>SoftwareCategory creation</h1>

<form action="{{ path('component_softwarecategory_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('component_softwarecategory') }}">
            Back to the list
        </a>
    </li>
</ul>
